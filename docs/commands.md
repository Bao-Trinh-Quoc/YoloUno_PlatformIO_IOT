# to compile the code, just run the command below in the terminal, make sure you are in the project directory, and have platformio installed in your system. 

```bash
pio run
```

# To upload code

```bash
pio run -t upload
```

# To compile, upload, and monitor the serial output

```bash
pio run -t upload -t monitor
```
